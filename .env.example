# Required: comma-separated list like "OWNER/REPO,OWNER/REPO"
EXPORT_REPOS=OWNER/REPO,OWNER/REPO

# Optional: override export locations
EXPORT_OUT_ROOT=export
EXPORT_RAW_ROOT=export/raw
EXPORT_PROFILE_DIR=export/browser_profile

# Optional: GitHub token (prefer `gh auth login`)
# GH_TOKEN=
